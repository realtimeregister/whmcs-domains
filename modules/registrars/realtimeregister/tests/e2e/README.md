## Run tests:
To run these tests, you need to create a .env from .env-example, fillout the required fields.
Setup of whms should be normal, e.g., install this plugin and make sure it is working.

You run the test by executing `npx playwright test` , add `--ui` to visually follow the tests.

